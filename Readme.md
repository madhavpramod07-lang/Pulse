# Pulse

A simple Python automation bot that:

- Fetches a random motivational quote
- Fetches weather information
- Generates a daily summary
- Saves the summary to a text file
- Sends an email automatically
- Uses GitHub Actions for scheduling

Built with Python, Requests,Gmail SMTP, Git, and GitHub.
